##################################################################################
# DON'T MODIFY THIS FILE
# File was generated by "yarn environment:init"
# File will be overwritten on next run of this script
# Please use values.override.yaml to modify helm chart behavior
# make sure your custom changes doesn't conflict with values.yaml
##################################################################################

########################################################################################
# Initial configuration file for OpenCRVS installation
########################################################################################
# Some properties are not defined in this file and should be provided as key/value at
# installation time:
# - hostname: valid DNS name for opencrvs
# - countryconfig.image.name: Countryconfig image repository
# - countryconfig.image.tag: Countryconfig image tag
environment_type: production

hpa:
  enabled: false

env:
  APN_SERVICE_URL: "http://apm-server.opencrvs-deps-qa.svc.cluster.local:8200"
  QA_ENV: true
  MINIO_BUCKET: ocrvs
influxdb:
  host: influxdb-0.influxdb.opencrvs-deps-qa.svc.cluster.local
elasticsearch:
  auth_mode: auto
  host: elasticsearch.opencrvs-deps-qa.svc.cluster.local


minio:
  auth_mode: use_secret
  host: minio-0.minio.opencrvs-deps-qa.svc.cluster.local

mongodb:
  auth_mode: auto
  host: mongodb-0.mongodb.opencrvs-deps-qa.svc.cluster.local

redis:
  auth_mode: use_secret
  host: redis-0.redis.opencrvs-deps-qa.svc.cluster.local

postgres:
  auth_mode: auto
  host: postgres-0.postgres.opencrvs-deps-qa.svc.cluster.local

imagePullSecrets:
  # Default value for credentials created while yarn environment:init
  - name: dockerhub-credentials

countryconfig:
  secrets:
    smtp-config:
      - ALERT_EMAIL
      - SENDER_EMAIL_ADDRESS
      - SMTP_HOST
      - SMTP_PASSWORD
      - SMTP_PORT
      - SMTP_SECURE
      - SMTP_USERNAME

postgres_on_deploy:
  secrets:
    reference-data-db-secret:
      - REFERENCE_DATA_POSTGRES_USER
      - REFERENCE_DATA_POSTGRES_PASSWORD
      - REFERENCE_DATA_EDITOR_USER
      - REFERENCE_DATA_EDITOR_PASSWORD

dashboards:
  use_default_credentials: false
  enabled: true